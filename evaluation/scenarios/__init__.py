"""Scenario authoring + generation package.

The scenario suite is generated by :mod:`evaluation.scenarios.generator` from
tier templates × phrase bank × topic/medium × language_variant, then written
to ``evaluation/scenarios/tier{1..7}_*.json``. The runner (Step 3) reads
those files plus a selection profile from ``selections/`` to choose which
scenarios to execute.
"""
