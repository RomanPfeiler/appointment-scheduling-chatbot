name: tier1_2_only
description: Tiers 1+2 only — the time-short fallback per EVALUATION_FRAMEWORK.md §12
tiers: [1, 2]
max_per_tier: 20
include_ids: []
exclude_ids: []
seed: 7
reps: 3
notes: |
  Cut-list profile. The framework explicitly names the easy tiers as the
  irreducible core if time is short ("the irreducible core is Steps 1-6 with
  the cooperative persona only ... and the baseline-vs-+NLP comparison on
  Tiers 1-2 first" — §12 cut-list). Use this when there is not enough budget
  to run `representative` against both stages.
