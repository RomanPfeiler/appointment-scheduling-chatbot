name: representative
description: 10 scenarios per tier, 3 reps — realistic-cost preset for reporting
tiers: [1, 2, 3, 4, 5, 6, 7]
max_per_tier: 10
include_ids: []
exclude_ids: []
seed: 42
reps: 3
notes: |
  The default "report-grade" profile. ~70 scenarios × 3 reps × 2 stages
  (baseline / +NLP) ≈ 420 conversations. Tight enough to fit in a working day
  on the current per-turn budget; wide enough to give paired-test power on
  every tier (EVALUATION_FRAMEWORK.md §10).
