name: full
description: All scenarios across all tiers — the ~500-scenario target (§12)
tiers: [1, 2, 3, 4, 5, 6, 7]
max_per_tier: 999
include_ids: []
exclude_ids: []
seed: 1
reps: 3
notes: |
  The full evaluation pass. With ~500 scenarios × 3 reps × 2 stages = 3000
  conversations, expect 25-100 hours of wall time at current per-turn cost
  (§12). Reserve this for the final report run. First lever to cut if
  infeasible: drop reps to 1 for low-variance tiers; then subset Tiers 1-2.
