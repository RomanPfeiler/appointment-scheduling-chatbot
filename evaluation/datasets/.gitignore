# Raw HF dataset caches live outside the repo (~/.cache/huggingface/...).
# Nothing in this directory should be a fetched original; only mined intermediates.
# Commit mined/*.jsonl deliberately for reproducibility — they are small.

# Ignore any accidental cache or parquet drops
*.parquet
*.arrow
huggingface/
